# TiRTC Go API Samples

These two clients consume `github.com/tangeai/tirtc-client-go/v2 v2.4.0-alpha.4`. Credentials are read from the environment; both programs require absolute `--cache-dir` and `--output-dir` arguments.
