# TiRTC Java API Samples

Build with `./mvnw package`; macOS ARM64 uses `-Dtirtc.native.classifier=macos-aarch64`. RTC and cloud storage credentials are read from the environment. Both clients require absolute `--cache-dir` and `--output-dir` paths.
