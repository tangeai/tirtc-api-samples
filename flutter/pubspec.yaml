name: tirtc_example
description: Two-page downlink demo for the tirtc plugin.
publish_to: 'none'
version: 0.1.0-dev

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  mobile_scanner: 7.4.0
  timezone: 0.9.4
  tirtc_flutter: 2.4.0-alpha.4
dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
