# TiRTC Android API Sample

This directory is generated from the Android Example maintained in the TiRTC repository.

The default SDK dependency is `com.tange.ai:tirtc:2.2.9`.

Open this directory as a standalone Gradle project and run the `example` application.
